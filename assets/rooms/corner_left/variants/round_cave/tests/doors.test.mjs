import test from 'node:test';import assert from 'node:assert/strict';import * as T from 'three';import {buildRoom} from '../source/room.mjs';
const hits=(r,o,d,far)=>new T.Raycaster(new T.Vector3(...o),new T.Vector3(...d),.01,far).intersectObject(r,true);
test('CL017 all nine candidate combinations have real open throats and clear paths',()=>{for(const south of [-2,0,2])for(const west of [-2,0,2]){const r=buildRoom({details:true,doors:{south,west}});r.updateMatrixWorld(true);for(const h of [.3,1.6,2.9])for(const u of [-.85,0,.85]){assert.equal(hits(r,[south*.5+u,h,-4.05],[0,0,1],3).length,0,`south ${south},${west}`);assert.equal(hits(r,[-4.05,h,west*.5+u],[1,0,0],3).length,0,`west ${south},${west}`);}}});
test('CL017 closed candidates are solid, invalid selection rejected',()=>{const r=buildRoom({doors:{south:null,west:null}});r.updateMatrixWorld(true);for(const off of [-1,0,1]){assert.ok(hits(r,[off,1.5,-4.05],[0,0,1],1).length);assert.ok(hits(r,[-4.05,1.5,off],[1,0,0],1).length)}assert.throws(()=>buildRoom({doors:{south:3,west:0}}));});
