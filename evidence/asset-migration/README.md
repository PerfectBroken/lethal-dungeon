# 2026-09-11 房间资产迁移验证

三个正式GLB及直通房开门历史GLB与迁移前SHA256完全一致。registry-red为迁移前缺失配置的失败记录，registry-green为正式目录验证结果。

C#：Domain 195、Architecture 14全部通过。房间几何/导出：尽头房33、直通房40、左转房22全部通过；尽头房结果位于dead-end-green.txt。仅修复迁移路径，没有更改几何或降低断言。

实际浏览器渲染检查：尽头房V0.26（3021）、直通房R27封墙版（3012）、左转房R28（3013）均正常加载；镜头灯光参数保留，模型字节一致。尽头房React页面通过Vite挂载；每个预览隔离缓存，避免同时运行时依赖优化结果互相覆盖。端口仅是本次验证端口。

Git LFS管理二进制，git lfs fsck通过。本机Git LFS工具保存于.git/lfs/bin，过滤器与推送钩子使用它；其他机器按README安装Git LFS。

资产仍需后续团结集成；此次不代表运行时随机开门或服务器美术变体选择已实现。
