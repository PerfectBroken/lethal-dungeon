# 执行状态与后续验收

2026-09-07。此文件记录实际进度，不将计划当作结果。

## 已完成

1. 背包倍率与探险时钟完成Green，未修改原有42个测试的预期；保留Red记录。
2. GitHub Actions工作流已上传：push、PR和手动触发，锁定SDK/依赖，Release构建，完整运行两套测试，保留TRX。
3. 新增4个CI配置检查和3个引擎配置检查，均先验证失败再补配置。本地最终42+13=55个检查全部通过，0跳过。
4. [团结验证工程](../engine/Validation/README.md)已准备：版本文件、包manifest、测试asmdef及3个待执行的编辑器导入用例。Domain插件已在开发机由本仓库构建复制，二进制不纳入Git。

5. 三维布局核心已完成：6种原型、候选门位对接、占用与门口检查、有限回退及独立校验；100个种子的跨层布局通过。本地最新总计142+14=156个检查通过，0跳过。

6. 环路扩展已完成：新增四向连接房与可配置最少环路数；100个种子的18房间布局均形成2个独立环路和跨层支路，无碰撞。环路当前以同层为主，尚非跨层大环。

7. 长支线末端连接替代局部小环作为主预览：先生成两条4～6房间支线，再BFS规划实体连接段；100种子均完成40房间、两个大环和跨层支路，全图没有四房间捷径。连接段仍是8米连接房灰盒，跨层长环未实现。

8. 种子规则seed-rules-v1已实现：主种子独立派生1～4个大环及布局种子，布局重试不重抽环数；100种子全部通过对应环数、房间规模、长环和空间检查。

## 当前阻塞

| 环节 | 事实 | 下一步 |
|---|---|---|
| CI云端执行 | GitHub账号因账单问题被锁定，job未启动 | 账号所有者在GitHub账单设置中解决后，重新运行Actions；不需要修改业务代码 |
| 团结编辑器 | 本机未安装；官方1.10.2 Apple Silicon安装包约5.07GB，已开始下载 | 完成下载和安装，登录团结账号并激活适用许可证，再执行导入测试 |
| 微信验证 | 开发者工具尚未安装，尚无本项目的AppID和真机验证记录 | 安装工具并登录；使用可用小游戏AppID建立开发预览，记录设备和结果 |
| 财宝闭环 | 用户正在设计财宝 | 等待物品信息和规则，继续规范→测试→实现；不预先决定占格、堆叠与分配 |

本轮不修改账号账单、不购买许可证、不使用虚构AppID。编辑器要求激活许可证，见[官方说明](https://docs.unity.cn/cn/tuanjiemanual/Manual/LicensesAndActivation.html)。

## 引擎与微信逐项验收

| 顺序 | 先行验收条件 | 当前结果 |
|---|---|---|
| E01 编辑器导入 | 目标编辑器打开工程，无编译错误；确认Domain DLL源自当前提交 | 未运行 |
| E02 编辑器测试 | 3个导入用例通过，生成真实EditMode报告 | 未运行 |
| E03 本地运行探针 | 先定义并测试探针契约，再建立简单3D场景；显示占格、倍率和探险时钟；明确本地验证标签 | 未实现 |
| E04 微信导出 | 启用MiniGame微信平台，确认SDK实际版本，导出无错误 | 未运行 |
| E05 开发者工具 | 导入wechat/minigame，验证启动、画面、输入与调试日志 | 未运行 |
| E06 微信真机 | 扫码启动，记录机型/系统/微信版本，验证加载、显示和切后台恢复 | 未运行 |
| E07 最小联机 | 先给房间屏障及同步协议写规范和测试；两个真实客户端共享服务器时钟，全员到达才开始 | 未实现 |

按[官方部署说明](https://docs.unity.cn/cn/tuanjiemanual/Manual/UploadWeixinMiniGame.html)，开发者工具运行与手机预览分开测试；多人参与测试需要相应AppID和测试人员权限。这里的执行步骤不是发布审核或正式上线。

## 后续灰盒闭环

前述技术链验证通过、财宝规则明确后，按模块逐个落实：队伍准备/到达屏障→物品拾取与搬运→装车→撤离→结算。首个场景使用占位几何体，再加入一种机制清楚的怪物验证风险。仍未确认的撤离读条、资产分配和战斗规则继续保留待定，不因建立场景自动获批。

## 独立探索支路更新

seed-rules-v2默认每环保障2条探索支路，各2～4房间；先预留预算，后生成大环，最后保护支路尽头。1～4环原型规模为32/56/80/104房间，v1仍可显式复现。100个种子通过；本地142+14=156项检查通过。104房间的探索节奏和微信性能尚未在引擎中验证。
